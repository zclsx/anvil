{
  "extends": "./tsconfig.json",
  "include": [
    "src",
    "electron",
    "tests",
    "playwright.config.ts",
    "vite.config.ts",
    "vitest.config.ts"
  ],
  "exclude": ["node_modules", "dist", "dist-electron", "release"]
}
